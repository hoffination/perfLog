angular.module("perfLog")
	.controller("navbarController", [function() {
		// Controller Defaults
		const DEFAULTS = {};

		// Public Exports
		const PUBLIC_EXPORTS = {};

		const $navbarCtrl = angular.merge(this, angular.merge(DEFAULTS, PUBLIC_EXPORTS));

	}]);
